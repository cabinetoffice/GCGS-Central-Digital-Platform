[
  {
    "name": "${name}",
    "image": "${image}",
    "cpu": ${cpu},
    "memory": ${memory},
    "portMappings": [],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${lg_name}",
        "awslogs-region": "${lg_region}",
        "awslogs-stream-prefix": "${lg_prefix}"
      }
    },
    "environment": [
        {"name": "ASPNETCORE_ENVIRONMENT", "value": "${aspcore_environment}"}
    ],
    "secrets": [
        {"name": "MIGRATIONS_CONNECTION_STRING", "valueFrom": "${conn_string_location}"}
    ],
    "volumesFrom": [],
    "mountPoints": [],
    "essential": true
  }
]
