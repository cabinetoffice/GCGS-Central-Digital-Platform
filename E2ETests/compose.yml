version: '3.8'

services:
  e2e-tests:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: e2e_tests
    volumes:
      - .:/app
    working_dir: /app
    networks:
      - cdp-sirsi
    environment:
      TestSettings__BaseUrl: http://gateway:8090
      TestSettings__ApiUrl: http://gateway:8082
      TestSettings__Email:
      TestSettings__Password:
      TestSettings__SecretKey:

networks:
  cdp-sirsi:
    external: true
