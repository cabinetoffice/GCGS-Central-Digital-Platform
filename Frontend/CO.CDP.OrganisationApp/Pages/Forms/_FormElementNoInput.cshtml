@model FormElementNoInputModel

@RenderHeading(Model.Heading, Model.Caption, Model.IsFirstQuestion, Model.Options?.Layout?.Heading?.Size)

@if (!string.IsNullOrWhiteSpace(Model.Description))
{
    @Html.Raw(Model.Description)
}