@using CDP.OrganisationApp.Constants
@inject IFlashMessageService flashMessageService

@if (flashMessageService.PeekFlashMessage(FlashMessageTypes.Success) != null)
{
    <div class="govuk-notification-banner govuk-notification-banner--success" role="alert" aria-labelledby="govuk-notification-banner-title" data-module="govuk-notification-banner">
        <div class="govuk-notification-banner__header">
            <h2 class="govuk-notification-banner__title" id="govuk-notification-banner-title">
                Success
            </h2>
        </div>
        @{
            var message = flashMessageService.GetFlashMessage(FlashMessageTypes.Success);
        }
        @if(message != null) {
            <div class="govuk-notification-banner__content">
                <p class="govuk-notification-banner__heading">@Html.Raw(message.Heading)</p>

                @if (message.Description != null)
                {
                    <p>@Html.Raw(message.Description)</p>
                }
            </div>
        }
    </div>
}

@if (flashMessageService.PeekFlashMessage(FlashMessageTypes.Important) != null)
{
    <div class="govuk-notification-banner" aria-labelledby="govuk-notification-banner-title" data-module="govuk-notification-banner">
        <div class="govuk-notification-banner__header">
            <h2 class="govuk-notification-banner__title" id="govuk-notification-banner-title">
                Important
            </h2>
        </div>
        @{
            var message = flashMessageService.GetFlashMessage(FlashMessageTypes.Important);
        }
        @if (message != null)
        {
            <div class="govuk-notification-banner__content">
                <p class="govuk-notification-banner__heading">@Html.Raw(message.Heading)</p>

                @if (message.Description != null)
                {
                    <p>@Html.Raw(message.Description)</p>
                }
            </div>
        }        
    </div>
}
